//Author: Nate
//Settings are used on a per trip basis and are created in the same trip view
const   
    express = require('express')
    settingsRouter = new express.Router()
    Setting = require('..models/Settings.js')

settingsRouter.route()